{
  "build": {
    "steps": [
      {
        "command": "uvicorn backend.main:app --host 0.0.0.0 --port ${PORT:-8000}"
      }
    ]
  },
  "providers": {
    "python": {
      "version": "3.11.9"
    }
  }
}